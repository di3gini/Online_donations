export const selectCountries = ({ donations }) => donations.countries
export const selectInstitutions = ({ donations }) => donations.institutions
export const selectDonations = ({ donations }) => donations.donations
